/**
 * Tailwind v4 uses `@theme` in `src/app/globals.css`.
 * This stub exists so `prettier-plugin-tailwindcss` can resolve a config during `pre-commit`.
 */
export default {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
};
