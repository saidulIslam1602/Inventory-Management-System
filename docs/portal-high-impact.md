# Portal & ops — high-impact backlog

Tracked suggestions from the “high impact / medium effort” brainstorm. Update this file when you ship or reprioritize.

## Done

| Item                                             | Notes                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Notification deep links**                      | `Notification.actionHref` (migration `20260215100000_notification_action_href`). PO + digest notifications set paths; `/me` list shows **Open** + **Mark read** / **Mark all read**.                                                                                                                                                                                 |
| **Global search (⌘K / Ctrl+K)**                  | `GET /api/search`, `GlobalSearchCommand` — products, POs, suppliers, **customers** (`Customer`), projects (incl. ad-hoc client fields), employees.                                                                                                                                                                                                                   |
| **PO receive batch helpers**                     | `PurchaseOrderReceiveForm`: **Fill remaining**, **Clear quantities**, **Post all pending lines** (single server round-trip).                                                                                                                                                                                                                                         |
| **Mark read when opening a link**                | `/me` notification **Open** calls `markNotificationRead` in the background when the row was unread.                                                                                                                                                                                                                                                                  |
| **Header notification dropdown**                 | `HeaderNotificationsMenu` + `GET /api/notifications/recent` — preview latest items, open target, **Mark all read**, link to portal.                                                                                                                                                                                                                                  |
| **Search: barcode / id / exact SKU → movements** | API matches `barcode`, product `id` (cuid), exact SKU; primary open is **movements** `?product=` when the query is an exact identifier match, otherwise **edit**.                                                                                                                                                                                                    |
| **Role-aware search**                            | **STAFF** does not get employee hits in `/api/search`.                                                                                                                                                                                                                                                                                                               |
| **Receive: Enter on last line**                  | Last quantity field **Enter** submits the receive form (`requestSubmit`).                                                                                                                                                                                                                                                                                            |
| **Email daily digest**                           | `/me` opt-in `emailDigestDaily` + same body as in-app digest via nodemailer (`SMTP_*`). `GET /api/cron/digest-email` with `Authorization: Bearer CRON_SECRET` (22h cooldown per user via `_lastDigestEmailAt` in preferences JSON).                                                                                                                                  |
| **PWA (manifest + offline)**                     | `manifest.ts` + `public/sw.js`: precache **`/`**, `/login`, `/offline.html`; **SWR** for `/` + `/login`; **cache-first** `/_next/static/*` (no `no-store` puts); other **navigate** → network-first → `aqila-ims-pages-v1` when allowed; **logout** → `CLEAR_AUTH_PAGE_CACHES` (delete `PAGES`, trim `SHELL` to login+offline). Bump `PAGES` after breaking deploys. |
| **Search: project clients**                      | `/api/search` matches `Project.clientName` & `clientPhone`; Cmd+K project rows show client line when set.                                                                                                                                                                                                                                                            |
| **Receive: mobile tap targets**                  | Batch actions + quantity fields use `min-h-11`, `text-base` on small screens, full-width primary submit.                                                                                                                                                                                                                                                             |
| **/me notification filters**                     | Chips: All / PO / Digest / Other; **Hide read**; loads 60 rows.                                                                                                                                                                                                                                                                                                      |
| **Customer master + CRUD**                       | `Customer` model, `Project.customerId`, `/customers`, `/customers/[id]`, **`/customers/new`**, **`/customers/[id]/edit`** (ADMIN/MANAGER), `createCustomer` / `updateCustomer` in `src/lib/actions/customers.ts`, active toggle on edit; Cmd+K + new project picker; seed links demo projects.                                                                       |

## Next (prioritized ideas)

— _None from this backlog; add new items as they come up._

## Related migrations

- `20250512120000_init_baseline` — full schema from empty DB.
- `20260215100000_notification_action_href` — `notifications.actionHref`.
- `20260216120000_customer_master` — `customers` table, `projects.customerId`.
